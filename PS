1. Caesar Cipher
from Crypto.Cipher import AES
def caesar(text, shift):
    result = ""
    for char in text:
        if char.isalpha():
            base = ord('A') if char.isupper() else ord('a')
            result += chr((ord(char) - base + shift) % 26 + base)
        else:
            result += char
    return result
text = "HELLO"
encrypted = caesar(text, 3)
decrypted = caesar(encrypted, -3)
print("Original :", text)
print("Encrypted:", encrypted)
print("Decrypted:", decrypted)

Output:
Original : HELLO
Encrypted: KHOOR
Decrypted: HELLO

=============================================================================================================================================================================================================
2. Playfair Cipher
def generate_matrix(key):
    key = key.upper().replace("J", "I")
    seen = []
    for ch in key + "ABCDEFGHIKLMNOPQRSTUVWXYZ":
        if ch not in seen:
            seen.append(ch)
    return [seen[i*5:(i+1)*5] for i in range(5)]
def find_pos(matrix, ch):
    for r, row in enumerate(matrix):
        if ch in row:
            return r, row.index(ch)
def playfair_encrypt(text, key):
    matrix = generate_matrix(key)
    text = text.upper().replace("J", "I").replace(" ", "")
    pairs = []
    i = 0
    while i < len(text):
        a = text[i]
        b = text[i+1] if i+1 < len(text) else 'X'
        if a == b:
            pairs.append((a, 'X'))
            i += 1
        else:
            pairs.append((a, b))
            i += 2
    result = ""
    for a, b in pairs:
        r1,c1 = find_pos(matrix, a)
        r2,c2 = find_pos(matrix, b)
        if r1 == r2:
            result += matrix[r1][(c1+1)%5] + matrix[r2][(c2+1)%5]
        elif c1 == c2:
            result += matrix[(r1+1)%5][c1] + matrix[(r2+1)%5][c2]
        else:
            result += matrix[r1][c2] + matrix[r2][c1]
    return result
print("Encrypted:", playfair_encrypt("HELLO", "KEY"))

Output:
Encrypted: DAXGM

============================================================================================================================================================================================================
3. Hill Cipher
import numpy as np
def hill_encrypt(text, key_matrix):
    text = text.upper()
    nums = [ord(c) - ord('A') for c in text]
    vector = np.array(nums).reshape(-1, 1)
    result = np.dot(key_matrix, vector) % 26
    return ''.join(chr(int(n) + ord('A')) for n in result.flatten())

key = np.array([[3, 3], [2, 5]])
text = "HI"
encrypted = hill_encrypt(text, key)
print("Original :", text)
print("Encrypted:", encrypted)

Output:
Original : HI
Encrypted: DL

===============================================================================================================================================================================================================
4. Rail Fence Cipher
def rail_fence_encrypt(text, rails):
    fence = [[] for _ in range(rails)]
    rail, direction = 0, 1
    for char in text:
        fence[rail].append(char)
        if rail == 0:
            direction = 1
        elif rail == rails - 1:
            direction = -1
        rail += direction
    return ''.join(''.join(row) for row in fence)
def rail_fence_decrypt(cipher, rails):
    n = len(cipher)
    pattern = []
    rail, direction = 0, 1
    for i in range(n):
        pattern.append(rail)
        if rail == 0: direction = 1
        elif rail == rails - 1: direction = -1
        rail += direction
    indices = sorted(range(n), key=lambda x: pattern[x])
    result = [''] * n
    for i, ch in zip(indices, cipher):
        result[i] = ch
    return ''.join(result)
text = "HELLOWORLD"
enc = rail_fence_encrypt(text, 3)
dec = rail_fence_decrypt(enc, 3)
print("Original :", text)
print("Encrypted:", enc)
print("Decrypted:", dec)

Output:
Original : HELLOWORLD
Encrypted: HOLELWRDLO
Decrypted: HELLOWORLD

===============================================================================================================================================================================
5. Columnar Transposition Cipher
def columnar_encrypt(text, key):
    text = text.replace(" ", "").upper()
    cols = len(key)
    rows = -(-len(text) // cols)          # ceil division
    text = text.ljust(rows * cols, 'X')   # pad with X
    grid = [text[i*cols:(i+1)*cols] for i in range(rows)]
    order = sorted(range(cols), key=lambda x: key[x])
    return ''.join(''.join(row[c] for row in grid) for c in order)
def columnar_decrypt(cipher, key):
    cols = len(key)
    rows = len(cipher) // cols
    order = sorted(range(cols), key=lambda x: key[x])
    grid = {}
    idx = 0
    for c in order:
        grid[c] = list(cipher[idx:idx+rows])
        idx += rows
    return ''.join(''.join(grid[c][r] for c in range(cols)) for r in range(rows))
text = "HELLOWORLD"
key  = [3, 1, 4, 2]
enc  = columnar_encrypt(text, key)
dec  = columnar_decrypt(enc, key)
print("Original :", text)
print("Encrypted:", enc)
print("Decrypted:", dec)

Output:
Original : HELLOWORLD
Encrypted: ELXLHXORWD
Decrypted: HELLOWORLD

===========================================================================================================================================================================
Digital Signature Standard (DSS) using Python

from Crypto.PublicKey import DSA
from Crypto.Signature import DSS
from Crypto.Hash import SHA256
key = DSA.generate(2048)
private_key = key
public_key = key.publickey()
message = "Hello, this is a digital signature example"
msg_bytes = message.encode()
hash_obj = SHA256.new(msg_bytes)
signer = DSS.new(private_key, 'fips-186-3')
signature = signer.sign(hash_obj)
print("Signature:", signature)
verifier = DSS.new(public_key, 'fips-186-3')
try:
    verifier.verify(hash_obj, signature)
    print("Signature is valid")
except ValueError:
    print("Signature is not valid")
    
Output (Example)
Signature: b'...binary data...'
Signature is valid



==========================================================================================================================================================================================

📦 Install Required Library
bashpip install pycryptodome numpy


PYTHON PROGRAM PROCEDURES (Pycrypt Libraries / Logic)
1. Caesar Cipher
         Take plaintext and key (shift value).
         Convert each letter to its ASCII value.
         Shift the letter by key value.
         Wrap around if it exceeds alphabet range.
         Convert back to character → ciphertext.
         For decryption, shift in reverse.
2. Playfair Cipher
         Remove duplicate letters from key.
         Create 5×5 matrix (combine I/J).
         Split plaintext into letter pairs.
         If same letters in pair → insert ‘X’.
         Apply rules:
         Same row → shift right
         Same column → shift down
         Rectangle → swap columns
         Combine letters → ciphertext.
3. Hill Cipher
         Choose key matrix (n×n).
         Convert plaintext letters → numbers (A=0…Z=25).
         Split into blocks of size n.
         Multiply block with key matrix.
        Take mod 26 of result.
        Convert numbers → ciphertext letters.
4. Rail Fence Cipher
        Choose number of rails (rows).
         Write plaintext in zigzag pattern.
         Read row-wise to get ciphertext.
         For decryption, reconstruct zigzag.
5. Columnar Transposition
         Choose a keyword.
        Write plaintext in rows under keyword.
        Number columns based on alphabetical order of key.
        Read columns in sorted order.
        Combine → ciphertext.
        TOOL-BASED PROCEDURES (CrypTool / Snort)
6. DES Algorithm (CrypTool)
         Open CrypTool.
         Enter plaintext.
         Select DES algorithm.
         Enter key (56-bit).
         Click encrypt → ciphertext generated.
         Use same key for decryption.
7. AES Algorithm (CrypTool)
       Open CrypTool.
       Enter plaintext.
      Choose AES (128/192/256-bit).
      Enter key.
      Click encrypt → ciphertext.
      Decrypt using same key.
8. RSA Algorithm (CrypTool)
       Open CrypTool.
       Generate public & private keys.
       Enter plaintext.
       Encrypt using public key.
       Decrypt using private key.
9. Diffie-Hellman Key Exchange
      Choose prime number (p) and base (g).
      User A selects private key → computes public key.
      User B selects private key → computes public key.
      Exchange public keys.
      Compute shared secret key using formula.
      Both get same key.
10. SHA-1 Message Digest
      Open CrypTool.
      Enter message.
      Select SHA-1 hashing.
      Generate hash value (160-bit).
      Output is fixed-length digest.
11. MD5 Message Digest
      Open CrypTool.
      Enter message.
      Select MD5 hashing.
      Generate hash value (128-bit).
      Used for integrity checking.
12. Digital Signature (DSS)
     Import required modules (DSA, SHA256, DSS).
     Generate DSA key pair (private + public key).
     Take the message to be signed.
     Convert message into bytes.
     Create hash of the message using SHA-256.
     Sign the hash using the private key.
     Send message + signature.
     Verify signature using public key.
     Display whether signature is valid or not.
13. Intrusion Detection System (Snort)
     Install Snort tool.
     Configure rules file.
     Start Snort in monitoring mode.
      Capture network packets.
     Detect suspicious activity.
       Generate alerts/logs.





1. Caesar Cipher – Viva Questions & Answers


What is Caesar Cipher?
Caesar Cipher is a substitution cipher where each letter is shifted by a fixed number.
Who invented Caesar Cipher?
Julius Caesar invented it.
What type of cipher is Caesar Cipher?
Monoalphabetic substitution cipher.
What is the key in Caesar Cipher?
The shift value.
What is encryption?
Converting plaintext into ciphertext.
What is decryption?
Converting ciphertext back into plaintext.
What happens if the shift exceeds 26?
It wraps around the alphabet using modulo operation.
Is Caesar Cipher secure?
No, it is weak against brute-force attacks
What is plaintext?
Original readable message.
What is ciphertext?
Encrypted unreadable message. 



2. Playfair Cipher – Viva Questions & Answers
What is Playfair Cipher?
A digraph substitution cipher that encrypts two letters together.
Why is Playfair stronger than Caesar Cipher?
Because it encrypts pairs of letters instead of single letters.
What size matrix is used in Playfair Cipher?
5 × 5 matrix.
Why are I and J combined?
To fit 25 letters into the matrix.
What happens if two letters in a pair are same?
An extra letter like X is inserted.
What is the key used for?
To generate the matrix.
What rule is used for same row letters?
Shift right.
What rule is used for same column letters?
Shift downward.
What happens in rectangle rule?
Columns are swapped.
What type of cipher is Playfair Cipher?
Symmetric key cipher. 



3. Hill Cipher – Viva Questions & Answers
What is Hill Cipher?
A polygraphic substitution cipher based on matrix multiplication.
Which mathematical concept is used?
Linear algebra and matrices.
What is the key in Hill Cipher?
A square matrix.
Why is modulo 26 used?
Because there are 26 alphabets.
How are letters represented?
A=0 to Z=25.
What is required for decryption?
Inverse key matrix.
What type of cipher is Hill Cipher?
Block cipher.
What happens if determinant is not invertible mod 26?
Decryption becomes impossible.
What is the advantage of Hill Cipher?
Provides better security than simple substitution ciphers.
Which library is used here?
NumPy. 


4. Rail Fence Cipher – Viva Questions & Answers
What is Rail Fence Cipher?
A transposition cipher using zigzag arrangement.
What is the key in Rail Fence Cipher?
Number of rails.
What type of cipher is it?
Transposition cipher.
How is encryption performed?
By writing text in zigzag pattern.
How is ciphertext generated?
By reading row-wise.
What is plaintext?
Original message.
What is decryption?
Reconstructing zigzag pattern to get original text.
Is Rail Fence Cipher secure?
No, it is a simple cipher.
What is the advantage of this cipher?
Easy to implement.
What is the output of the experiment?
Encrypted and decrypted text. 



5. Columnar Transposition Cipher – Viva Questions & Answers
What is Columnar Transposition Cipher?
A cipher where letters are rearranged column-wise.
What is used as key?
A keyword or numeric order.
What type of cipher is this?
Transposition cipher.
How is encryption done?
Text is written row-wise and read column-wise.
Why is padding used?
To fill empty spaces in the matrix.
What character is commonly used for padding?
X.
What determines column order?
Alphabetical order of the key.
What is the advantage of transposition cipher?
Letter frequencies remain hidden by rearrangement.
What is decryption?
Reconstructing original arrangement of text.
What is the output?
Encrypted and decrypted message. 



6. DES Algorithm – Viva Questions & Answers
What is DES?
DES stands for Data Encryption Standard.
What type of algorithm is DES?
Symmetric key algorithm.
What is the key size of DES?
56 bits.
How many rounds are used in DES?
16 rounds.
What type of cipher is DES?
Block cipher.
What is the block size of DES?
64 bits.
Why is DES considered insecure today?
Because of small key size.
What is symmetric encryption?
Same key is used for encryption and decryption.
Which tool is used in this experiment?
CrypTool.
What is the output of DES?
Ciphertext.



7. AES Algorithm – Viva Questions & Answers
What is AES?
AES stands for Advanced Encryption Standard.
What type of algorithm is AES?
Symmetric block cipher.
What key sizes are supported in AES?
128, 192, and 256 bits.
Why is AES more secure than DES?
Because it uses larger keys and stronger encryption.
What is the block size of AES?
128 bits.
Where is AES used?
Wi-Fi, banking, and secure communications.
Is AES symmetric or asymmetric?
Symmetric.
What is plaintext?
Original readable data.
What is ciphertext?
Encrypted unreadable data.
Which tool is used here?
CrypTool. 



8. RSA Algorithm – Viva Questions & Answers
What is RSA?
RSA is an asymmetric encryption algorithm.
What does RSA stand for?
Rivest, Shamir, and Adleman.
What keys are used in RSA?
Public key and private key.
Which key is used for encryption?
Public key.
Which key is used for decryption?
Private key.
What type of algorithm is RSA?
Public key cryptography algorithm.
What is the advantage of RSA?
Secure key exchange.
What mathematical concept is used?
Prime number factorization.
Is RSA slower than AES?
Yes.
Where is RSA used?
Digital signatures and secure communication.



9. Diffie-Hellman Key Exchange – Viva Questions & Answers
What is Diffie-Hellman?
A method for securely exchanging cryptographic keys.
Is Diffie-Hellman an encryption algorithm?
No, it is a key exchange algorithm.
What are the public values used?
Prime number (p) and base (g).
What is the purpose of private key?
To generate public and shared keys.
What is shared secret key?
A common key generated by both users.
What is the advantage of Diffie-Hellman?
Secure key exchange over insecure channels.
Which attack affects Diffie-Hellman?
Man-in-the-middle attack.
Is the shared key same for both users?
Yes.
What type of cryptography is used?
Public key cryptography.
Where is Diffie-Hellman used?
VPN and HTTPS connections.



10. SHA-1 Message Digest – Viva Questions & Answers
What is SHA-1?
SHA-1 is a hashing algorithm.
What is the output size of SHA-1?
160 bits.
What is hashing?
Converting data into fixed-size digest.
Is SHA-1 reversible?
No.
What is message digest?
Fixed-length hash value.
What is SHA-1 mainly used for?
Integrity checking.
Why is SHA-1 insecure today?
Because collisions can occur.
What is collision in hashing?
Two inputs producing same hash.
Is SHA-1 encryption?
No, it is hashing.
What is the output of SHA-1?
160-bit hash value.



11. MD5 Message Digest – Viva Questions & Answers
What is MD5?
A hashing algorithm producing 128-bit digest.
What is the output size of MD5?
128 bits
Is MD5 secure today?
No.
What is hashing used for?
Integrity verification.
Is MD5 reversible?
No.
What is collision?
Same hash generated for different inputs.
What is the advantage of MD5?
Fast hash generation.
What is the disadvantage of MD5?
Weak security.
Is MD5 encryption?
No.
Where is MD5 used?
File integrity checking.



12. Digital Signature Standard (DSS) – Viva Questions & Answers
What is DSS?
Digital Signature Standard used for digital signatures.
Which algorithm is used here?
DSA.
What is digital signature?
Electronic method to verify authenticity of data.
What is the purpose of SHA-256?
To generate hash of the message.
Which key signs the message?
Private key
Which key verifies the signature?
Public key.
What is authentication?
Verifying sender identity.
Wat is integrity?
Ensuring data is not modified.
What happens if verification fails?
Signature becomes invalid.
Which Python library is used?
PyCryptodome. 



13. Intrusion Detection System (Snort) – Viva Questions & Answers
What is Snort?
Snort is an intrusion detection and prevention tool.
What is IDS?
Intrusion Detection System.
What is the purpose of Snort?
To monitor and detect suspicious network activity.
What are Snort rules?
Conditions used to identify attacks.
What is packet sniffing?
Capturing and analyzing network packets.
What is an alert in Snort?
Warning generated when suspicious activity is detected.
What is intrusion?
Unauthorized access or attack on a system.
What mode is used in Snort experiment?
Monitoring mode.
Can Snort prevent attacks?
Yes, in IPS mode.
What is the output of Snort?
Alerts and log files. 


